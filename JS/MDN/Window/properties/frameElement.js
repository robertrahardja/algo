// The element which the window is embedded into.
// If the window isn't embedded into another document, 
// or if the document into which it's embedded has a different origin,
// the value is null instead.
window.frameElement

