// The navigator is a built-in JavaScript object 
// that provides information about the browser and its capabilities.
// It's a property of the global window object (though it's typically accessed directly as navigator without the window. prefix).
//
navigator.userAgent;
// returns the user agent string for the current browser
